class Obstacle{
  // Klassenattribute für Position, Länge und Breite. (length und width sind von processing besetze Wörter und können nicht als Variablennamen verwendet werden)
  // Und zum definieren ob das Rechteck liegend oder stehend gezeichnet wird.

  
  // Konstruktor
  Obstacle(float x, float y, boolean isVertical, float obLength){
  }
  
  // Methode zum zeichnen des Rechtecks
  void paint(){
  }
  
  // Methode die true zurück gibt, wenn die x UND y Parameter das Rechteck berühren.
  boolean isTouched(float x, float y){
  }
  
}
